As I mentioned, I have a teaching demo I've been working on where I haven't been able to figure out how to configure the interface.  It's a simulation of sequential investments.  One starts with an initial wealth, then bets a fraction of the previous outcome on a simulated investment that has a better than 50% chance of winning, It teaches how investing is affected by risk. 

It's written in python -- the libraries needed are in the requirements.txt file generated by the "pip" utility.  The graphics and web server are provided by the bokeh javascript wrappers, which I believe use Jinja2 web templates.

The folder attached contains two apps
dice_server/  - runs a static graphic using templates.  It's broken
c_server/  - runs the dynamic simulation.  It doesn't use custom html templates, using instead the default ones in Bokeh.  This is the application that needs an interface re-design, 
L